export function DemoBanner() {
  return (
    <div className="bg-indigo-600 text-white text-center py-2 px-4 text-sm font-medium">
      🔬 InspectFlow Demo — open DevTools, edit any style, then click{" "}
      <strong>Analyze →</strong> in the InspectFlow panel to sync it back to source.
    </div>
  );
}
